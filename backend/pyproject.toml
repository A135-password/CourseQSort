[tool.black]
line-length = 120
target-version = ["py313"]
extend-exclude = '''
/(
    migrations
  | \.pytest_cache
  | \.venv
  | venv
)/
'''

[tool.isort]
profile = "black"
line_length = 120
skip_glob = ["*/migrations/*.py"]
